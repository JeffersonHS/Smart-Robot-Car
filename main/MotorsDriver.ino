/*void GoingForward(unsigned char Speed) {
  analogWrite(speedPinA, Speed);
  analogWrite(speedPinB, Speed);
  //Motor A turn clockwise
  digitalWrite(pinI1, LOW);
  digitalWrite(pinI2, HIGH);
  //Motor B turn anticlockwise
  digitalWrite(pinI3, HIGH);
  digitalWrite(pinI4, LOW);
}

void GoingBackward(unsigned char Speed) {
  analogWrite(speedPinA, Speed);
  analogWrite(speedPinB, Speed);
  //Motor A turn anticlockwise
  digitalWrite(pinI1, HIGH);
  digitalWrite(pinI2, LOW);
  //Motor B turn clockwise
  digitalWrite(pinI3, LOW);
  digitalWrite(pinI4, HIGH);
}

void TurnRight(unsigned char Speed) {
  analogWrite(speedPinA, Speed);
  analogWrite(speedPinB, Speed);
  //Motor A turn anticlockwise
  digitalWrite(pinI1, HIGH);
  digitalWrite(pinI2, LOW);
  //Motor B turn anticlockwise
  digitalWrite(pinI3, HIGH);
  digitalWrite(pinI4, LOW);
}

void TurnLeft(unsigned char Speed) {
  analogWrite(speedPinA, Speed);
  analogWrite(speedPinB, Speed);
  //Motor A turn clockwise
  digitalWrite(pinI1, LOW);
  digitalWrite(pinI2, HIGH);
  //Motor B turn clockwise
  digitalWrite(pinI3, LOW);
  digitalWrite(pinI4, HIGH);
}

void Brake() { //Substitute for English
  analogWrite(speedPinA, 100);
  analogWrite(speedPinB, 100);
  //Motor A turn clockwise
  digitalWrite(pinI1, HIGH);
  digitalWrite(pinI2, HIGH);
  //Motor B turn clockwise
  digitalWrite(pinI3, HIGH);
  digitalWrite(pinI4, HIGH);
}*/
